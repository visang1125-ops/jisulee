# Vercel 배포 문제 해결 가이드

## 🚨 현재 문제

1. **레이아웃이 깨짐** - CSS가 제대로 로드되지 않음
2. **데이터 로딩 실패** - API 연결 실패

## ✅ 즉시 확인 사항

### 1. Vercel 환경 변수 확인

**Vercel 대시보드 → Settings → Environment Variables**

다음 환경 변수가 설정되어 있는지 확인:
- `VITE_API_URL`: 백엔드 서버 URL (예: `https://your-backend.railway.app`)

### 2. 브라우저 개발자 도구 확인

**F12 → Console 탭:**
```javascript
// 환경 정보 확인
console.log('API URL:', import.meta.env.VITE_API_URL);
```

**F12 → Network 탭:**
- CSS 파일이 로드되는지 확인
- API 요청이 실패하는지 확인
- CORS 오류가 있는지 확인

### 3. 백엔드 서버 확인

백엔드 서버 URL + `/health` 접속:
```
https://your-backend.railway.app/health
```

응답이 `{"status":"ok"}`인지 확인

## 🔧 해결 방법

### 방법 1: 환경 변수 설정

1. Vercel 대시보드 접속
2. 프로젝트 → Settings → Environment Variables
3. `VITE_API_URL` 추가
   - Key: `VITE_API_URL`
   - Value: 백엔드 서버 URL
   - Environment: Production, Preview, Development 모두 선택
4. Save 후 재배포

### 방법 2: 백엔드 CORS 설정

백엔드 서버(Railway/Render)에 환경 변수 추가:
```
ALLOWED_ORIGINS=https://your-app.vercel.app
```

### 방법 3: 빌드 확인

로컬에서 빌드 테스트:
```bash
npm run build:client
```

빌드가 성공하는지 확인

## 📋 체크리스트

- [ ] Vercel 환경 변수 `VITE_API_URL` 설정
- [ ] 백엔드 서버가 실행 중인지 확인
- [ ] 백엔드 `/health` 엔드포인트 응답 확인
- [ ] 브라우저 개발자 도구에서 오류 확인
- [ ] CSS 파일이 로드되는지 확인 (Network 탭)
- [ ] API 요청이 올바른 URL로 가는지 확인

## 🆘 여전히 문제가 있는 경우

### 레이아웃이 깨진 경우
1. 브라우저 캐시 삭제 (Ctrl+Shift+Delete)
2. 시크릿 모드에서 테스트
3. Network 탭에서 CSS 파일 로드 확인
4. Vercel 빌드 로그 확인

### 데이터 로딩 실패
1. Console 탭에서 에러 메시지 확인
2. Network 탭에서 API 요청 확인
3. API URL이 올바른지 확인
4. CORS 오류가 있는지 확인

## 💡 디버깅 코드

브라우저 콘솔에서 실행:
```javascript
// API 연결 테스트
fetch((import.meta.env.VITE_API_URL || '/api') + '/health')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);

// 환경 변수 확인
console.log('VITE_API_URL:', import.meta.env.VITE_API_URL);
```

